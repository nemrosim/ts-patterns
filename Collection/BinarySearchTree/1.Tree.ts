/**
 * A data structure where nodes
 * have a 1:N parent-child relationship
 *
 * Properties of Trees:
 * - 0 or 1 parent node;
 * - 0-N child nodes (binary, trinary, k-ary)
 * - Leaf nodes have no children
 * - One data item per node
 *
 *
 */
class Tree {

}
